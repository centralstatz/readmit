% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsr_read.R
\name{hsr_read}
\alias{hsr_read}
\title{Parse a Hospital-Specific Report (HSR)}
\usage{
hsr_read(path, format = NULL, validate = TRUE, ...)
}
\arguments{
\item{path}{File path to a report.}

\item{format}{Optional format override. Defaults to \code{NULL}, in which case
\code{\link[=hsr_detect_format]{hsr_detect_format()}} is used.}

\item{validate}{Should the parsed object be validated before returning?
Defaults to \code{TRUE}.}

\item{...}{Reserved for future parser-specific options.}
}
\value{
A \code{readmit_hsr} object.
}
\description{
Parses a Hospital-Specific Report (HSR) into a lightweight internal object
that can be reused by downstream helpers. Phase 1 support is limited to the
legacy Excel-based HSR format used through FY2025.

The parsed object preserves current legacy section outputs as much as
possible, while also retaining raw parser metadata for debugging and future
format support.

If \code{format} is left \code{NULL}, \code{hsr_detect_format()} is used to identify the
supported parser to apply.
}
\examples{
report <- hsr_mock_reports("FY2025_HRRP_MockHSR.xlsx")
parsed <- hsr_read(report)
parsed
}
